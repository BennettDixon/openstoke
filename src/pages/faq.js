import { FAQPage } from 'components'
import { containers } from 'ui-kit'

const FAQ = props => {
  return (
    <containers.PrimaryWrap>
      <FAQPage />
    </containers.PrimaryWrap>
  )
}

export default FAQ
